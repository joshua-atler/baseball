export interface Media {
    title: string;
    imageURL: string;
    videoURL: string;
}

export interface Article {
    headline: string;
    author: string;
    imageURL: string;
    date: string;
    body: string;
    slug: string;
}
